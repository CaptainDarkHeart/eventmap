{
  "name": "eventmap",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "check": "astro check",
    "validate-events": "node scripts/validate-events.mjs",
    "prune-past-events": "node scripts/prune-past-events.mjs",
    "check-urls": "node scripts/check-urls.mjs",
    "deploy": "node scripts/prune-past-events.mjs && astro build && wrangler deploy"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.7.4",
    "astro": "^7.3.2"
  },
  "allowScripts": {
    "esbuild": true
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.10",
    "typescript": "^6.0.3",
    "wrangler": "^4.131.0"
  }
}
